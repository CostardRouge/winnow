node_modules/
.next/
out/
build/
dist/
*.log
npm-debug.log*
# Every local env file stays out; .env.dist is the tracked, documented template.
.env
.env.*
!.env.dist
# Private keys / certs, wherever they land.
*.pem
*.key
*.p12
*.keystore
# Editor state — per-developer, never shared.
.idea/
.vscode/
.DS_Store
coverage/
.tsbuildinfo
*.tsbuildinfo
nas/
data/
# Local Postgres dumps written by the backup sidecar / scripts/pg-backup.sh
backups/

# Artefact Redis (tests locaux)
dump.rdb
/nas-final
/nas-incoming
